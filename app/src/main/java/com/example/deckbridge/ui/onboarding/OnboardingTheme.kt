package com.example.deckbridge.ui.onboarding

import androidx.compose.ui.graphics.Color

/** Premium dark palette for first-run onboarding (independent of system light theme). */
object OnboardingTheme {
    val background = Color(0xFF050508)
    val card = Color(0xFF14141A)
    val cardElevated = Color(0xFF1C1C24)
    val accent = Color(0xFF2D62FF)
    val accentDim = Color(0xFF1E408C)
    val textPrimary = Color(0xFFF4F4F8)
    val textSecondary = Color(0xFFC8C8D4)
    val textMuted = Color(0xFF7A7A8A)
    val listRow = Color(0xFF1E1E26)
}
