package com.example.deckbridge.lan

/**
 * Placeholder mobile id for “open invite” QR sessions; must match the LAN agent’s `QR_INVITE_DEVICE_ID`.
 */
const val QR_INVITE_DEVICE_ID: String = "__deckbridge_qr_invite__"
